import { NextPage } from 'next';
import GuestLayout from '../components/GuestLayout';

const Signin: NextPage = () => {
  return <GuestLayout title='Sign In'>Login</GuestLayout>;
};

export default Signin;
